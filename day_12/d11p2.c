#include <stdio.h>
int main() {
    int x,y;
    printf("enter the two numbers:");
    scanf("%d%d",&x,&y);
    if (x > y) {
        printf("the maximum of two numbers is %d",x);
    }
    else {
        return  printf("the maximum of two numbers is %d",y);
    }
      
}